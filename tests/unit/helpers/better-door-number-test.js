import { betterDoorNumber } from 'lesson-03/helpers/better-door-number';
import { module, test } from 'qunit';

module('Unit | Helper | better door number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = betterDoorNumber([42]);
  assert.ok(result);
});
