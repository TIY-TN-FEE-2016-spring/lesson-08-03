<ul class="light-bar">
  {{#each lights as |light position|}}
    <li>
      {{light}}
      {{#if (eq activeLightPosition position)}}
        <div class="fa fa-star"></div>
      {{/if}}
    </li>
  {{/each}}
</ul>

{{#if isSpinning}}
<button {{action stopSpinning}}>Stop Spinning</button>
{{else}}
<button {{action startSpinning}}>Start Spinning</button>
{{/if}}
